For the 'agentic_memory' module: Review and improve src/codomyrmex/agentic_memory/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/agentic_memory/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/agentic_memory/ -v' to verify tests pass.
For the 'agents' module: Review and improve src/codomyrmex/agents/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/agents/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/agents/ -v' to verify tests pass.
For the 'ai_gateway' module: Review and improve src/codomyrmex/ai_gateway/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/ai_gateway/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/ai_gateway/ -v' to verify tests pass.
For the 'api' module: Review and improve src/codomyrmex/api/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/api/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/api/ -v' to verify tests pass.
For the 'audio' module: Review and improve src/codomyrmex/audio/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/audio/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/audio/ -v' to verify tests pass.
For the 'auth' module: Review and improve src/codomyrmex/auth/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/auth/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/auth/ -v' to verify tests pass.
For the 'autograd' module: Review and improve src/codomyrmex/autograd/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/autograd/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/autograd/ -v' to verify tests pass.
For the 'bio_simulation' module: Review and improve src/codomyrmex/bio_simulation/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/bio_simulation/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/bio_simulation/ -v' to verify tests pass.
For the 'cache' module: Review and improve src/codomyrmex/cache/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/cache/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/cache/ -v' to verify tests pass.
For the 'calendar_integration' module: Review and improve src/codomyrmex/calendar_integration/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/calendar_integration/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/calendar_integration/ -v' to verify tests pass.
For the 'cerebrum' module: Review and improve src/codomyrmex/cerebrum/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/cerebrum/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/cerebrum/ -v' to verify tests pass.
For the 'ci_cd_automation' module: Review and improve src/codomyrmex/ci_cd_automation/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/ci_cd_automation/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/ci_cd_automation/ -v' to verify tests pass.
For the 'cli' module: Review and improve src/codomyrmex/cli/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/cli/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/cli/ -v' to verify tests pass.
For the 'cloud' module: Review and improve src/codomyrmex/cloud/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/cloud/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/cloud/ -v' to verify tests pass.
For the 'coding' module: Review and improve src/codomyrmex/coding/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/coding/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/coding/ -v' to verify tests pass.
For the 'collaboration' module: Review and improve src/codomyrmex/collaboration/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/collaboration/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/collaboration/ -v' to verify tests pass.
For the 'compression' module: Review and improve src/codomyrmex/compression/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/compression/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/compression/ -v' to verify tests pass.
For the 'concurrency' module: Review and improve src/codomyrmex/concurrency/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/concurrency/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/concurrency/ -v' to verify tests pass.
For the 'config_audits' module: Review and improve src/codomyrmex/config_audits/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/config_audits/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/config_audits/ -v' to verify tests pass.
For the 'config_management' module: Review and improve src/codomyrmex/config_management/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/config_management/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/config_management/ -v' to verify tests pass.
For the 'config_monitoring' module: Review and improve src/codomyrmex/config_monitoring/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/config_monitoring/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/config_monitoring/ -v' to verify tests pass.
For the 'container_optimization' module: Review and improve src/codomyrmex/container_optimization/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/container_optimization/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/container_optimization/ -v' to verify tests pass.
For the 'containerization' module: Review and improve src/codomyrmex/containerization/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/containerization/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/containerization/ -v' to verify tests pass.
For the 'cost_management' module: Review and improve src/codomyrmex/cost_management/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/cost_management/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/cost_management/ -v' to verify tests pass.
For the 'crypto' module: Review and improve src/codomyrmex/crypto/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/crypto/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/crypto/ -v' to verify tests pass.
For the 'dark' module: Review and improve src/codomyrmex/dark/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/dark/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/dark/ -v' to verify tests pass.
For the 'data_curation' module: Review and improve src/codomyrmex/data_curation/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/data_curation/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/data_curation/ -v' to verify tests pass.
For the 'data_lineage' module: Review and improve src/codomyrmex/data_lineage/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/data_lineage/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/data_lineage/ -v' to verify tests pass.
For the 'data_visualization' module: Review and improve src/codomyrmex/data_visualization/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/data_visualization/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/data_visualization/ -v' to verify tests pass.
For the 'database_management' module: Review and improve src/codomyrmex/database_management/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/database_management/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/database_management/ -v' to verify tests pass.
For the 'defense' module: Review and improve src/codomyrmex/defense/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/defense/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/defense/ -v' to verify tests pass.
For the 'demos' module: Review and improve src/codomyrmex/demos/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/demos/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/demos/ -v' to verify tests pass.
For the 'dependency_injection' module: Review and improve src/codomyrmex/dependency_injection/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/dependency_injection/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/dependency_injection/ -v' to verify tests pass.
For the 'deployment' module: Review and improve src/codomyrmex/deployment/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/deployment/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/deployment/ -v' to verify tests pass.
For the 'distillation' module: Review and improve src/codomyrmex/distillation/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/distillation/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/distillation/ -v' to verify tests pass.
For the 'distributed_training' module: Review and improve src/codomyrmex/distributed_training/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/distributed_training/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/distributed_training/ -v' to verify tests pass.
For the 'docs_gen' module: Review and improve src/codomyrmex/docs_gen/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/docs_gen/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/docs_gen/ -v' to verify tests pass.
For the 'documentation' module: Review and improve src/codomyrmex/documentation/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/documentation/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/documentation/ -v' to verify tests pass.
For the 'documents' module: Review and improve src/codomyrmex/documents/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/documents/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/documents/ -v' to verify tests pass.
For the 'dpo' module: Review and improve src/codomyrmex/dpo/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/dpo/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/dpo/ -v' to verify tests pass.
For the 'edge_computing' module: Review and improve src/codomyrmex/edge_computing/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/edge_computing/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/edge_computing/ -v' to verify tests pass.
For the 'email' module: Review and improve src/codomyrmex/email/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/email/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/email/ -v' to verify tests pass.
For the 'embodiment' module: Review and improve src/codomyrmex/embodiment/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/embodiment/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/embodiment/ -v' to verify tests pass.
For the 'encryption' module: Review and improve src/codomyrmex/encryption/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/encryption/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/encryption/ -v' to verify tests pass.
For the 'environment_setup' module: Review and improve src/codomyrmex/environment_setup/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/environment_setup/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/environment_setup/ -v' to verify tests pass.
For the 'eval_harness' module: Review and improve src/codomyrmex/eval_harness/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/eval_harness/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/eval_harness/ -v' to verify tests pass.
For the 'events' module: Review and improve src/codomyrmex/events/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/events/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/events/ -v' to verify tests pass.
For the 'evolutionary_ai' module: Review and improve src/codomyrmex/evolutionary_ai/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/evolutionary_ai/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/evolutionary_ai/ -v' to verify tests pass.
For the 'examples' module: Review and improve src/codomyrmex/examples/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/examples/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/examples/ -v' to verify tests pass.
For the 'exceptions' module: Review and improve src/codomyrmex/exceptions/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/exceptions/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/exceptions/ -v' to verify tests pass.
For the 'feature_flags' module: Review and improve src/codomyrmex/feature_flags/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/feature_flags/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/feature_flags/ -v' to verify tests pass.
For the 'feature_store' module: Review and improve src/codomyrmex/feature_store/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/feature_store/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/feature_store/ -v' to verify tests pass.
For the 'file_system' module: Review and improve src/codomyrmex/file_system/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/file_system/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/file_system/ -v' to verify tests pass.
For the 'finance' module: Review and improve src/codomyrmex/finance/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/finance/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/finance/ -v' to verify tests pass.
For the 'formal_verification' module: Review and improve src/codomyrmex/formal_verification/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/formal_verification/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/formal_verification/ -v' to verify tests pass.
For the 'fpf' module: Review and improve src/codomyrmex/fpf/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/fpf/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/fpf/ -v' to verify tests pass.
For the 'git_analysis' module: Review and improve src/codomyrmex/git_analysis/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/git_analysis/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/git_analysis/ -v' to verify tests pass.
For the 'git_operations' module: Review and improve src/codomyrmex/git_operations/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/git_operations/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/git_operations/ -v' to verify tests pass.
For the 'graph_rag' module: Review and improve src/codomyrmex/graph_rag/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/graph_rag/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/graph_rag/ -v' to verify tests pass.
For the 'ide' module: Review and improve src/codomyrmex/ide/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/ide/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/ide/ -v' to verify tests pass.
For the 'identity' module: Review and improve src/codomyrmex/identity/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/identity/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/identity/ -v' to verify tests pass.
For the 'image' module: Review and improve src/codomyrmex/image/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/image/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/image/ -v' to verify tests pass.
For the 'interpretability' module: Review and improve src/codomyrmex/interpretability/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/interpretability/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/interpretability/ -v' to verify tests pass.
For the 'llm' module: Review and improve src/codomyrmex/llm/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/llm/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/llm/ -v' to verify tests pass.
For the 'logging_monitoring' module: Review and improve src/codomyrmex/logging_monitoring/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/logging_monitoring/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/logging_monitoring/ -v' to verify tests pass.
For the 'logistics' module: Review and improve src/codomyrmex/logistics/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/logistics/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/logistics/ -v' to verify tests pass.
For the 'logit_processor' module: Review and improve src/codomyrmex/logit_processor/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/logit_processor/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/logit_processor/ -v' to verify tests pass.
For the 'lora' module: Review and improve src/codomyrmex/lora/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/lora/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/lora/ -v' to verify tests pass.
For the 'maintenance' module: Review and improve src/codomyrmex/maintenance/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/maintenance/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/maintenance/ -v' to verify tests pass.
For the 'market' module: Review and improve src/codomyrmex/market/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/market/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/market/ -v' to verify tests pass.
For the 'matmul_kernel' module: Review and improve src/codomyrmex/matmul_kernel/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/matmul_kernel/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/matmul_kernel/ -v' to verify tests pass.
For the 'meme' module: Review and improve src/codomyrmex/meme/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/meme/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/meme/ -v' to verify tests pass.
For the 'model_context_protocol' module: Review and improve src/codomyrmex/model_context_protocol/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/model_context_protocol/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/model_context_protocol/ -v' to verify tests pass.
For the 'model_merger' module: Review and improve src/codomyrmex/model_merger/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/model_merger/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/model_merger/ -v' to verify tests pass.
For the 'model_ops' module: Review and improve src/codomyrmex/model_ops/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/model_ops/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/model_ops/ -v' to verify tests pass.
For the 'module_template' module: Review and improve src/codomyrmex/module_template/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/module_template/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/module_template/ -v' to verify tests pass.
For the 'multimodal' module: Review and improve src/codomyrmex/multimodal/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/multimodal/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/multimodal/ -v' to verify tests pass.
For the 'nas' module: Review and improve src/codomyrmex/nas/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/nas/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/nas/ -v' to verify tests pass.
For the 'networking' module: Review and improve src/codomyrmex/networking/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/networking/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/networking/ -v' to verify tests pass.
For the 'networks' module: Review and improve src/codomyrmex/networks/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/networks/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/networks/ -v' to verify tests pass.
For the 'neural' module: Review and improve src/codomyrmex/neural/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/neural/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/neural/ -v' to verify tests pass.
For the 'operating_system' module: Review and improve src/codomyrmex/operating_system/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/operating_system/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/operating_system/ -v' to verify tests pass.
For the 'orchestrator' module: Review and improve src/codomyrmex/orchestrator/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/orchestrator/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/orchestrator/ -v' to verify tests pass.
For the 'peft' module: Review and improve src/codomyrmex/peft/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/peft/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/peft/ -v' to verify tests pass.
For the 'performance' module: Review and improve src/codomyrmex/performance/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/performance/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/performance/ -v' to verify tests pass.
For the 'physical_management' module: Review and improve src/codomyrmex/physical_management/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/physical_management/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/physical_management/ -v' to verify tests pass.
For the 'plugin_system' module: Review and improve src/codomyrmex/plugin_system/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/plugin_system/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/plugin_system/ -v' to verify tests pass.
For the 'privacy' module: Review and improve src/codomyrmex/privacy/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/privacy/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/privacy/ -v' to verify tests pass.
For the 'prompt_engineering' module: Review and improve src/codomyrmex/prompt_engineering/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/prompt_engineering/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/prompt_engineering/ -v' to verify tests pass.
For the 'quantization' module: Review and improve src/codomyrmex/quantization/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/quantization/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/quantization/ -v' to verify tests pass.
For the 'quantum' module: Review and improve src/codomyrmex/quantum/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/quantum/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/quantum/ -v' to verify tests pass.
For the 'relations' module: Review and improve src/codomyrmex/relations/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/relations/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/relations/ -v' to verify tests pass.
For the 'release' module: Review and improve src/codomyrmex/release/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/release/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/release/ -v' to verify tests pass.
For the 'rlhf' module: Review and improve src/codomyrmex/rlhf/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/rlhf/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/rlhf/ -v' to verify tests pass.
For the 'scrape' module: Review and improve src/codomyrmex/scrape/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/scrape/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/scrape/ -v' to verify tests pass.
For the 'search' module: Review and improve src/codomyrmex/search/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/search/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/search/ -v' to verify tests pass.
For the 'security' module: Review and improve src/codomyrmex/security/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/security/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/security/ -v' to verify tests pass.
For the 'semantic_router' module: Review and improve src/codomyrmex/semantic_router/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/semantic_router/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/semantic_router/ -v' to verify tests pass.
For the 'serialization' module: Review and improve src/codomyrmex/serialization/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/serialization/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/serialization/ -v' to verify tests pass.
For the 'simulation' module: Review and improve src/codomyrmex/simulation/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/simulation/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/simulation/ -v' to verify tests pass.
For the 'skills' module: Review and improve src/codomyrmex/skills/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/skills/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/skills/ -v' to verify tests pass.
For the 'slm' module: Review and improve src/codomyrmex/slm/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/slm/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/slm/ -v' to verify tests pass.
For the 'softmax_opt' module: Review and improve src/codomyrmex/softmax_opt/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/softmax_opt/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/softmax_opt/ -v' to verify tests pass.
For the 'soul' module: Review and improve src/codomyrmex/soul/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/soul/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/soul/ -v' to verify tests pass.
For the 'spatial' module: Review and improve src/codomyrmex/spatial/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/spatial/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/spatial/ -v' to verify tests pass.
For the 'ssm' module: Review and improve src/codomyrmex/ssm/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/ssm/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/ssm/ -v' to verify tests pass.
For the 'static_analysis' module: Review and improve src/codomyrmex/static_analysis/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/static_analysis/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/static_analysis/ -v' to verify tests pass.
For the 'synthetic_data' module: Review and improve src/codomyrmex/synthetic_data/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/synthetic_data/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/synthetic_data/ -v' to verify tests pass.
For the 'system_discovery' module: Review and improve src/codomyrmex/system_discovery/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/system_discovery/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/system_discovery/ -v' to verify tests pass.
For the 'telemetry' module: Review and improve src/codomyrmex/telemetry/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/telemetry/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/telemetry/ -v' to verify tests pass.
For the 'templating' module: Review and improve src/codomyrmex/templating/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/templating/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/templating/ -v' to verify tests pass.
For the 'terminal_interface' module: Review and improve src/codomyrmex/terminal_interface/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/terminal_interface/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/terminal_interface/ -v' to verify tests pass.
For the 'testing' module: Review and improve src/codomyrmex/testing/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/testing/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/testing/ -v' to verify tests pass.
For the 'text_to_sql' module: Review and improve src/codomyrmex/text_to_sql/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/text_to_sql/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/text_to_sql/ -v' to verify tests pass.
For the 'tokenizer' module: Review and improve src/codomyrmex/tokenizer/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/tokenizer/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/tokenizer/ -v' to verify tests pass.
For the 'tool_use' module: Review and improve src/codomyrmex/tool_use/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/tool_use/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/tool_use/ -v' to verify tests pass.
For the 'tree_sitter' module: Review and improve src/codomyrmex/tree_sitter/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/tree_sitter/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/tree_sitter/ -v' to verify tests pass.
For the 'utils' module: Review and improve src/codomyrmex/utils/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/utils/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/utils/ -v' to verify tests pass.
For the 'validation' module: Review and improve src/codomyrmex/validation/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/validation/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/validation/ -v' to verify tests pass.
For the 'vector_store' module: Review and improve src/codomyrmex/vector_store/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/vector_store/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/vector_store/ -v' to verify tests pass.
For the 'video' module: Review and improve src/codomyrmex/video/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/video/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/video/ -v' to verify tests pass.
For the 'wallet' module: Review and improve src/codomyrmex/wallet/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/wallet/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/wallet/ -v' to verify tests pass.
For the 'website' module: Review and improve src/codomyrmex/website/ — add missing docstrings to all public functions, ensure mcp_tools.py exists with @mcp_tool decorators (follow pattern in src/codomyrmex/auth/mcp_tools.py), add or expand zero-mock tests in src/codomyrmex/tests/unit/website/, and ensure README.md + AGENTS.md + SPEC.md exist in the module directory. Use 'uv' for all Python operations. Run 'uv run pytest src/codomyrmex/tests/unit/website/ -v' to verify tests pass.
