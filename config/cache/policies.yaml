# Cache Policy Configuration Template

policies:
  default:
    ttl: 3600
    max_size: 1000
    eviction: "lru"
  
  short_term:
    ttl: 300
    max_size: 500
  
  long_term:
    ttl: 86400
    max_size: 10000

eviction:
  strategy: "lru"  # lru, lfu, fifo
  max_memory: "512MB"


