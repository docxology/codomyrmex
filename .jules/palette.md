## 2026-08-01 - Missing ARIA Labels on Icon-only Buttons
**Learning:** Using `title` on icon-only buttons isn't always sufficient for all screen readers, explicit `aria-label` attributes guarantee better accessibility on interactive elements.
**Action:** Ensure both `title` (for visual tooltips) and `aria-label` (for screen readers) are used on icon-only buttons.
