## 2024-09-07 - Add ARIA Labels to Chat Input
**Learning:** Chat inputs with multiple interactive elements (like attach, send, and remove attachment buttons) require explicit `aria-label` attributes to be fully accessible for screen reader users, especially when relying on icons.
**Action:** Ensure all icon-only buttons and complex input areas have appropriate `aria-label` attributes applied.
