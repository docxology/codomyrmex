## 2026-08-08 - ARIA Labels on Icon-only Buttons
**Learning:** Icon-only buttons often lack ARIA labels and leave decorative SVGs visible to screen readers.
**Action:** Ensure all icon-only buttons include `aria-label` attributes and their decorative SVGs are marked with `aria-hidden="true"`.
