## 2026-08-19 - Missing ARIA Labels on Icon Buttons
**Learning:** Icon-only buttons using the UI component library often rely exclusively on 'title' attributes or visual context, lacking explicit screen-reader accessible names.
**Action:** Always verify icon-only buttons include an explicit 'aria-label' attribute, even if a 'title' or visual icon is present, to ensure proper accessibility for screen readers.
