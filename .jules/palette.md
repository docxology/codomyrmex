## 2026-08-09 - Icon-Only Button Accessibility
**Learning:** Icon-only buttons (like '×' or SVG icons) without 'aria-label' are inaccessible to screen readers, leaving users without context for critical actions like closing modals or detaching terminals.
**Action:** Always provide descriptive 'aria-label' attributes on buttons lacking visible text to ensure accessible names are announced.
