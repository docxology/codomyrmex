# SPEC — scripts/tool_use

## Purpose
Signpost and optional automation for `tool_use`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/tool_use/` and `docs/modules/tool_use/`.
