# SPEC — scripts/privacy

## Purpose
Signpost and optional automation for `privacy`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/privacy/` and `docs/modules/privacy/`.
