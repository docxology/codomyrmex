# SPEC — scripts/meme

## Purpose
Signpost and optional automation for `meme`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/meme/` and `docs/modules/meme/`.
