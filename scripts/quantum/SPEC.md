# SPEC — scripts/quantum

## Purpose
Signpost and optional automation for `quantum`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/quantum/` and `docs/modules/quantum/`.
