# SPEC — scripts/prompt_engineering

## Purpose
Signpost and optional automation for `prompt_engineering`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/prompt_engineering/` and `docs/modules/prompt_engineering/`.
