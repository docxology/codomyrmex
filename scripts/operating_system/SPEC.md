# SPEC — scripts/operating_system

## Purpose
Signpost and optional automation for `operating_system`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/operating_system/` and `docs/modules/operating_system/`.
