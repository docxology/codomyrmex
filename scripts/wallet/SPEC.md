# SPEC — scripts/wallet

## Purpose
Signpost and optional automation for `wallet`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/wallet/` and `docs/modules/wallet/`.
