# SPEC — scripts/simulation

## Purpose
Signpost and optional automation for `simulation`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/simulation/` and `docs/modules/simulation/`.
