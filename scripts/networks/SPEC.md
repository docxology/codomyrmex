# SPEC — scripts/networks

## Purpose
Signpost and optional automation for `networks`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/networks/` and `docs/modules/networks/`.
