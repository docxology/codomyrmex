# SPEC — scripts/release

## Purpose
Signpost and optional automation for `release`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/release/` and `docs/modules/release/`.
