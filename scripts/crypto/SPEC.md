# SPEC — scripts/crypto

## Purpose
Signpost and optional automation for `crypto`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/crypto/` and `docs/modules/crypto/`.
