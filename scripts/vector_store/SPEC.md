# SPEC — scripts/vector_store

## Purpose
Signpost and optional automation for `vector_store`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/vector_store/` and `docs/modules/vector_store/`.
