# SPEC — scripts/market

## Purpose
Signpost and optional automation for `market`. Extend this file when public script CLIs or stable entry points are added.

## Scope
- Library behavior is specified under `src/codomyrmex/market/` and `docs/modules/market/`.
