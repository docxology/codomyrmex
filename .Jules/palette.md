## 2024-07-24 - Missing ARIA labels on Header Buttons
**Learning:** Icon-only utility buttons in the header lacked ARIA labels and titles, hindering screen reader users.
**Action:** Always ensure icon-only buttons include descriptive `aria-label` attributes.
