# Git Visualization Submodule

Git repository visualization for commit graphs, branches, and history.

## Usage

```python
from codomyrmex.data_visualization.git import GitVisualizer
viz = GitVisualizer(repo_path)
viz.render_commit_graph()
```
