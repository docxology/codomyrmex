# Logit Processor Module

This module provides logit processing utilities for language model generation.

## Overview

The logit processor allows modifying logits during generation to apply penalties or biases.

## Available Tools

- `logit_processor_apply`: Modify a tensor of logits according to a specified configuration.
