# Kubernetes Submodule

Kubernetes orchestration and deployment management.

## Usage

```python
from codomyrmex.containerization.kubernetes import KubernetesOrchestrator
k8s = KubernetesOrchestrator(kubeconfig_path)
k8s.deploy("deployment.yaml")
```
