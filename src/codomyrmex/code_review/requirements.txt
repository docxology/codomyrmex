# Core dependencies for code review functionality
# Note: pyscn should be installed globally with pipx for security
# pipx install pyscn

# Traditional static analysis tools (optional fallbacks)
pylint>=2.17.0
flake8>=6.0.0
mypy>=1.0.0
bandit>=1.7.0
radon>=6.0.0
vulture>=2.7.0
safety>=2.3.0

# Configuration and file handling
tomli>=2.0.0  # For TOML config parsing

# Additional utilities
pathlib>=1.0.1  # Already in stdlib for Python 3.10+
typing-extensions>=4.0.0  # For better type hints

# Development dependencies (for testing the module)
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0

