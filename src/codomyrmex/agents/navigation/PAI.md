# Agent Navigation — PAI Integration

PAI consumers can use the navigation MCP tools during OBSERVE and THINK to
choose a provider, inspect module/documentation parity, and locate a tool
before requesting execution. The catalog is intentionally non-authoritative
for credentials and trust: execution still goes through the normal MCP trust
gateway and provider configuration checks.
