# AGENTS.md for ML Pipeline

- Always include docstrings for new functions, classes, and methods.
- Ensure all MCP tools are correctly decorated with `@mcp_tool`.
- Write zero-mock tests for all tools in `tests/unit/ml_pipeline/`.
