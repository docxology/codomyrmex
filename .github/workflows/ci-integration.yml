name: Integration Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  integration:
    name: Integration Test Suite
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "0.10.8"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --locked --group docs --all-extras --dev

      - name: Install native integration prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends espeak-ng
          docker pull python:3.9-slim
          docker pull bash:5.1

      - name: Run integration tests
        run: |
          uv run pytest -m integration \
            --timeout=120 \
            --no-header \
            -q \
            --tb=short \
            -x \
            2>&1 || true
        env:
          CODOMYRMEX_ENV: ci
          CODOMYRMEX_INTEGRATION: "1"

      - name: Run integration tests (strict)
        run: |
          uv run pytest tests/integration/ \
            --timeout=120 \
            --no-header \
            -q \
            --tb=short
        env:
          CODOMYRMEX_ENV: ci
          CODOMYRMEX_INTEGRATION: "1"
