For the '__pycache__' module: Write a thin orchestrator script in scripts/__pycache__/ (or improve existing), then go to src/codomyrmex/__pycache__/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'agentic_memory' module: Write a thin orchestrator script in scripts/agentic_memory/ (or improve existing), then go to src/codomyrmex/agentic_memory/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'agents' module: Write a thin orchestrator script in scripts/agents/ (or improve existing), then go to src/codomyrmex/agents/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'api' module: Write a thin orchestrator script in scripts/api/ (or improve existing), then go to src/codomyrmex/api/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'audits' module: Write a thin orchestrator script in scripts/audits/ (or improve existing), then go to src/codomyrmex/audits/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'auth' module: Write a thin orchestrator script in scripts/auth/ (or improve existing), then go to src/codomyrmex/auth/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'build_synthesis' module: Write a thin orchestrator script in scripts/build_synthesis/ (or improve existing), then go to src/codomyrmex/build_synthesis/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'cache' module: Write a thin orchestrator script in scripts/cache/ (or improve existing), then go to src/codomyrmex/cache/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'cerebrum' module: Write a thin orchestrator script in scripts/cerebrum/ (or improve existing), then go to src/codomyrmex/cerebrum/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'ci_cd_automation' module: Write a thin orchestrator script in scripts/ci_cd_automation/ (or improve existing), then go to src/codomyrmex/ci_cd_automation/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'cli' module: Write a thin orchestrator script in scripts/cli/ (or improve existing), then go to src/codomyrmex/cli/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'cloud' module: Write a thin orchestrator script in scripts/cloud/ (or improve existing), then go to src/codomyrmex/cloud/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'coding' module: Write a thin orchestrator script in scripts/coding/ (or improve existing), then go to src/codomyrmex/coding/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'collaboration' module: Write a thin orchestrator script in scripts/collaboration/ (or improve existing), then go to src/codomyrmex/collaboration/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'compression' module: Write a thin orchestrator script in scripts/compression/ (or improve existing), then go to src/codomyrmex/compression/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'concurrency' module: Write a thin orchestrator script in scripts/concurrency/ (or improve existing), then go to src/codomyrmex/concurrency/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'config_audits' module: Write a thin orchestrator script in scripts/config_audits/ (or improve existing), then go to src/codomyrmex/config_audits/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'config_management' module: Write a thin orchestrator script in scripts/config_management/ (or improve existing), then go to src/codomyrmex/config_management/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'config_monitoring' module: Write a thin orchestrator script in scripts/config_monitoring/ (or improve existing), then go to src/codomyrmex/config_monitoring/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'container_optimization' module: Write a thin orchestrator script in scripts/container_optimization/ (or improve existing), then go to src/codomyrmex/container_optimization/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'containerization' module: Write a thin orchestrator script in scripts/containerization/ (or improve existing), then go to src/codomyrmex/containerization/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'cost_management' module: Write a thin orchestrator script in scripts/cost_management/ (or improve existing), then go to src/codomyrmex/cost_management/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'data_lineage' module: Write a thin orchestrator script in scripts/data_lineage/ (or improve existing), then go to src/codomyrmex/data_lineage/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'data_visualization' module: Write a thin orchestrator script in scripts/data_visualization/ (or improve existing), then go to src/codomyrmex/data_visualization/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'database_management' module: Write a thin orchestrator script in scripts/database_management/ (or improve existing), then go to src/codomyrmex/database_management/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'demos' module: Write a thin orchestrator script in scripts/demos/ (or improve existing), then go to src/codomyrmex/demos/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'deployment' module: Write a thin orchestrator script in scripts/deployment/ (or improve existing), then go to src/codomyrmex/deployment/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'docs' module: Write a thin orchestrator script in scripts/docs/ (or improve existing), then go to src/codomyrmex/docs/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'documentation' module: Write a thin orchestrator script in scripts/documentation/ (or improve existing), then go to src/codomyrmex/documentation/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'documents' module: Write a thin orchestrator script in scripts/documents/ (or improve existing), then go to src/codomyrmex/documents/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'embodiment' module: Write a thin orchestrator script in scripts/embodiment/ (or improve existing), then go to src/codomyrmex/embodiment/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'encryption' module: Write a thin orchestrator script in scripts/encryption/ (or improve existing), then go to src/codomyrmex/encryption/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'environment_setup' module: Write a thin orchestrator script in scripts/environment_setup/ (or improve existing), then go to src/codomyrmex/environment_setup/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'events' module: Write a thin orchestrator script in scripts/events/ (or improve existing), then go to src/codomyrmex/events/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'evolutionary_ai' module: Write a thin orchestrator script in scripts/evolutionary_ai/ (or improve existing), then go to src/codomyrmex/evolutionary_ai/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'examples' module: Write a thin orchestrator script in scripts/examples/ (or improve existing), then go to src/codomyrmex/examples/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'exceptions' module: Write a thin orchestrator script in scripts/exceptions/ (or improve existing), then go to src/codomyrmex/exceptions/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'feature_flags' module: Write a thin orchestrator script in scripts/feature_flags/ (or improve existing), then go to src/codomyrmex/feature_flags/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'feature_store' module: Write a thin orchestrator script in scripts/feature_store/ (or improve existing), then go to src/codomyrmex/feature_store/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'file_system' module: Write a thin orchestrator script in scripts/file_system/ (or improve existing), then go to src/codomyrmex/file_system/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'formal_verification' module: Write a thin orchestrator script in scripts/formal_verification/ (or improve existing), then go to src/codomyrmex/formal_verification/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'fpf' module: Write a thin orchestrator script in scripts/fpf/ (or improve existing), then go to src/codomyrmex/fpf/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'git_operations' module: Write a thin orchestrator script in scripts/git_operations/ (or improve existing), then go to src/codomyrmex/git_operations/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'graph_rag' module: Write a thin orchestrator script in scripts/graph_rag/ (or improve existing), then go to src/codomyrmex/graph_rag/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'ide' module: Write a thin orchestrator script in scripts/ide/ (or improve existing), then go to src/codomyrmex/ide/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'inference_optimization' module: Write a thin orchestrator script in scripts/inference_optimization/ (or improve existing), then go to src/codomyrmex/inference_optimization/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'llm' module: Write a thin orchestrator script in scripts/llm/ (or improve existing), then go to src/codomyrmex/llm/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'logging_monitoring' module: Write a thin orchestrator script in scripts/logging_monitoring/ (or improve existing), then go to src/codomyrmex/logging_monitoring/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'logistics' module: Write a thin orchestrator script in scripts/logistics/ (or improve existing), then go to src/codomyrmex/logistics/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'maintenance' module: Write a thin orchestrator script in scripts/maintenance/ (or improve existing), then go to src/codomyrmex/maintenance/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'metrics' module: Write a thin orchestrator script in scripts/metrics/ (or improve existing), then go to src/codomyrmex/metrics/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'migration' module: Write a thin orchestrator script in scripts/migration/ (or improve existing), then go to src/codomyrmex/migration/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'model_context_protocol' module: Write a thin orchestrator script in scripts/model_context_protocol/ (or improve existing), then go to src/codomyrmex/model_context_protocol/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'model_ops' module: Write a thin orchestrator script in scripts/model_ops/ (or improve existing), then go to src/codomyrmex/model_ops/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'model_registry' module: Write a thin orchestrator script in scripts/model_registry/ (or improve existing), then go to src/codomyrmex/model_registry/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'module_template' module: Write a thin orchestrator script in scripts/module_template/ (or improve existing), then go to src/codomyrmex/module_template/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'multimodal' module: Write a thin orchestrator script in scripts/multimodal/ (or improve existing), then go to src/codomyrmex/multimodal/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'networking' module: Write a thin orchestrator script in scripts/networking/ (or improve existing), then go to src/codomyrmex/networking/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'notification' module: Write a thin orchestrator script in scripts/notification/ (or improve existing), then go to src/codomyrmex/notification/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'observability_dashboard' module: Write a thin orchestrator script in scripts/observability_dashboard/ (or improve existing), then go to src/codomyrmex/observability_dashboard/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'orchestrator' module: Write a thin orchestrator script in scripts/orchestrator/ (or improve existing), then go to src/codomyrmex/orchestrator/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'pai' module: Write a thin orchestrator script in scripts/pai/ (or improve existing), then go to src/codomyrmex/pai/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'pattern_matching' module: Write a thin orchestrator script in scripts/pattern_matching/ (or improve existing), then go to src/codomyrmex/pattern_matching/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'performance' module: Write a thin orchestrator script in scripts/performance/ (or improve existing), then go to src/codomyrmex/performance/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'physical_management' module: Write a thin orchestrator script in scripts/physical_management/ (or improve existing), then go to src/codomyrmex/physical_management/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'plugin_system' module: Write a thin orchestrator script in scripts/plugin_system/ (or improve existing), then go to src/codomyrmex/plugin_system/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'prompt_testing' module: Write a thin orchestrator script in scripts/prompt_testing/ (or improve existing), then go to src/codomyrmex/prompt_testing/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'reports' module: Write a thin orchestrator script in scripts/reports/ (or improve existing), then go to src/codomyrmex/reports/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'rna' module: Write a thin orchestrator script in scripts/rna/ (or improve existing), then go to src/codomyrmex/rna/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'scrape' module: Write a thin orchestrator script in scripts/scrape/ (or improve existing), then go to src/codomyrmex/scrape/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'security' module: Write a thin orchestrator script in scripts/security/ (or improve existing), then go to src/codomyrmex/security/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'serialization' module: Write a thin orchestrator script in scripts/serialization/ (or improve existing), then go to src/codomyrmex/serialization/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'skills' module: Write a thin orchestrator script in scripts/skills/ (or improve existing), then go to src/codomyrmex/skills/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'spatial' module: Write a thin orchestrator script in scripts/spatial/ (or improve existing), then go to src/codomyrmex/spatial/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'static_analysis' module: Write a thin orchestrator script in scripts/static_analysis/ (or improve existing), then go to src/codomyrmex/static_analysis/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'system_discovery' module: Write a thin orchestrator script in scripts/system_discovery/ (or improve existing), then go to src/codomyrmex/system_discovery/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'telemetry' module: Write a thin orchestrator script in scripts/telemetry/ (or improve existing), then go to src/codomyrmex/telemetry/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'templating' module: Write a thin orchestrator script in scripts/templating/ (or improve existing), then go to src/codomyrmex/templating/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'terminal_interface' module: Write a thin orchestrator script in scripts/terminal_interface/ (or improve existing), then go to src/codomyrmex/terminal_interface/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'tools' module: Write a thin orchestrator script in scripts/tools/ (or improve existing), then go to src/codomyrmex/tools/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'tree_sitter' module: Write a thin orchestrator script in scripts/tree_sitter/ (or improve existing), then go to src/codomyrmex/tree_sitter/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'utils' module: Write a thin orchestrator script in scripts/utils/ (or improve existing), then go to src/codomyrmex/utils/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'validation' module: Write a thin orchestrator script in scripts/validation/ (or improve existing), then go to src/codomyrmex/validation/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'verification' module: Write a thin orchestrator script in scripts/verification/ (or improve existing), then go to src/codomyrmex/verification/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'website' module: Write a thin orchestrator script in scripts/website/ (or improve existing), then go to src/codomyrmex/website/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'workflow_execution' module: Write a thin orchestrator script in scripts/workflow_execution/ (or improve existing), then go to src/codomyrmex/workflow_execution/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
For the 'workflow_testing' module: Write a thin orchestrator script in scripts/workflow_testing/ (or improve existing), then go to src/codomyrmex/workflow_testing/ and comprehensively add/improve all methods, zero-mock tests, and documentation (AGENTS.md, README.md, SPEC.md). Ensure the orchestrator is a working example of the improved module.
