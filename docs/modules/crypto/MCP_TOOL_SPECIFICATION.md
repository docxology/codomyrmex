# Crypto Module — MCP Tool Specification

No MCP tools are exposed for the crypto module. Cryptographic operations are security-sensitive
and should only be accessed through the Python API with proper authorization controls.
