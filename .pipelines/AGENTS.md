# AGENTS.md — `codomyrmex/.pipelines`

## Purpose
Placeholder mount point for CI/CD pipeline definitions. Pipeline logic lives in
the `ci_cd_automation` module (`src/codomyrmex/ci_cd_automation/`); GitHub
Actions workflows live in `.github/workflows/`.

## Layout
- Currently empty apart from this documentation pair.

## Gotchas
- If pipeline definitions are added, document each file here and keep the
  `ci_cd_automation` module README in sync.
