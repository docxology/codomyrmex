1. **Add strictly zero-mock tests for `mcp_tools.py` in `scrape` module.**
   - Create `src/codomyrmex/tests/unit/scrape/unit/test_mcp_tools.py` implementing zero-mock tests for `scrape_extract_content` and `scrape_text_similarity`.

2. **Check for missing docstrings in `mcp_tools.py` and fix them if any.**
   - Already done as it looks complete and typed correctly. Wait, `mcp_tools.py` actually contains docstrings. I'll make sure there's no missing ones.

3. **Check documentation (README.md, AGENTS.md, SPEC.md).**
   - The user asked to ensure they exist. I have already verified `README.md`, `AGENTS.md`, and `SPEC.md` exist and look correct. I'll make sure there's no small missing info.

4. **Run pre-commit instructions.**
   - Make sure proper testing, verification, review, and reflection are done using `pre_commit_instructions` tool.

5. **Commit and submit.**
   - Call `submit`.
